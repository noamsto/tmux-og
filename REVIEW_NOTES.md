# Review notes — feat/646-use-osc-133-pane-command-hooks-for-dead

| invariant/family | finding or thread IDs | observed head | fix commit | proof | disposition | rounds used |
| --- | --- | --- | --- | --- | --- | --- |
| (spec) osc-133 hook format claims | spec-critic medium: `#{session_id}` → `sh` garbage in `run-shell` (the `$N`→`$0` re-expansion trap); `#{hook_session_name}`/`#{hook_session_id}`/`#{hook_window_id}` empty (payloads only pane/window). Grouped-session `#{session_name}` resolution; fork-count on every prompt; non-manifest argv[0] inherited gap. Verdict `accept`, non-blocking notes. | — | — | spec `2026-09-16-osc-133-pane-command-dead-agent-design.md` corrected: measured table now names usable formats (`#{hook_pane}`, `#{session_name}`, `#{window_id}`), guard pinned to `#{session_name}`; short-circuit + grouped-session residual + inherited-manifest-gap notes added | fixed | 0 |

Note: the spec-critic's first `crew msg` was truncated mid-body at the bus writer; its second post carried the complete verdict (accept, 1 medium + 3 low). Both read; the complete one is the source of the ledger row above.

| (plan) script wiring | plan-critic round 1 `revise` (2 blocking): `tmux-shell-prompt` missing from `ogInternal` (config/tmux.conf.nix ~:704, `ogPartitionOk` assert forced) and from `RequiredScripts` (generator/paths/paths.go ~:33, `missingkey=error` + `FromPrefix`). Round 2 `accept` after both fixed (slots verified: `tmux-scratchpad`<`tmux-shell-prompt`<`tmux-smart-nav`; `tmux-session-picker`<`tmux-shell-prompt`<`tmux-splash-maybe`) + 3 notes folded (short-circuit residual, `session=` seeding, Script-Roles row). | — | — | plan `2026-09-16-osc-133-pane-command-dead-agent.md` fixed; plan-critic round 2 accept | fixed | 1 |
| (code) header-comment quoting drift | reviewer round 1 `revise` (1 MEDIUM, non-behavioral): `scripts/tmux-shell-prompt.sh:3` header named `#{q:pane_current_command}`/`#{q:session_name}` but the wiring uses `#{qs:…}` (wrap-required). Mechanical fix, no behavior change. | db01d63 | db01d63 | comment pinned to `#{qs:…}`; full gate re-run green (nix build .#default, nix flake check 76, nix build .#lint) | fixed (mechanical; no re-review per EVIDENCE_REVIEW) | 1 |
| (CI) darwin `pane_current_command` fixture | CI failure on aarch64-darwin (run 35117563830/job/104866758617): `not ok 2 nested prompt inside a live agent: not cleared`. Root cause verified against tmux e880cf63 (pinned rev) source: `osdep-linux.c` reads `/proc/<pgrp>/cmdline` (argv[0]); `osdep-darwin.c` reads `proc_pidinfo`/`pbsi_comm` (kernel process name), unaffected by `exec -a`'s argv[0] rewrite — a test bug, not a handler bug. | (this PR, pending) | (this PR, pending) | `tests/pane-shell-prompt.bats`: fake agent is now a real copied `bash` binary at `$TEST_HOME/bin/pi`, interpreting a script file, so the kernel process name is `pi` on both platforms; `nix build .#checks.x86_64-linux.osc-133-dead-agent-tests` green locally (6/6), shell-reviewer approve | fixed | 0 |
| (code) ownership guard fails open | reviewer round (PR #670 review 5225308323), should-fix: `scripts/lib-claude.sh` guard fell through to clearing when the caller's `$sess` was empty (`-n $sess` in the combined condition let it slip); the `session=` read loop also silently dropped a final line with no trailing newline (`read` returns nonzero, loop body never runs). | (this PR, pending) | (this PR, pending) | guard now fails closed on empty `$sess`; read loop takes `\|\| [[ -n $key ]]`; two new bats units (empty session; unterminated `session=` line) exercise both, sourcing `lib-claude.sh` with no live tmux server; shell-reviewer approve, gate green | fixed | 0 |
| (test) fixture drift from real writer | task item 3 (clarity, non-blocking): `seed_shell_state` hand-wrote `session=%s` with no link to the real writer's format. | (this PR, pending) | (this PR, pending) | added `seed_shell_state_via_writer`, used by the first test, threading `CLAUDE_STATUS_DIR`/`--session` into the raw `scripts/claude-status-update.sh` so a writer format drift breaks this suite too; other tests keep the hand-written fixture per the task's guidance | fixed | 0 |
